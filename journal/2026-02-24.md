# 2026-02-24

## 오늘 한 것
VMware에 Ubuntu Server를 설치했다.  
디스크는 40GB로 설정하고 LVM으로 진행했다.  
설치 과정 따라가면서 서버를 하나 직접 만들었다.  
재부팅 후 로그인까지 성공했다.

기본 명령어도 몇 개 실행해봤다.
whoami, pwd, ip a 같은 명령어로 상태를 확인했다.

## 막힌 부분
처음에 Git Bash에서 리눅스 명령어를 입력해서 안 됐다.
VM 안이랑 윈도우 터미널이 다르다는 걸 헷갈렸다.

설치 과정에서 선택지가 많아서 조금 긴장됐다.

## 해결
Ubuntu VM 터미널에서 명령어를 입력하니 정상적으로 동작했다.
설치 옵션은 대부분 기본값으로 진행했다.

## 느낀 점
직접 서버 하나 만든 느낌이라 신기했다.
처음엔 어려울 줄 알았는데 생각보다 할 만했다.
이제 리눅스를 실제로 만지고 있다는 느낌이 난다.

## 내일 할 것
- 리눅스 기본 명령어 연습
- 폴더 만들기
- 파일 만들기
- VM 다시 실행해보기